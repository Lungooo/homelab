---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - namespace: traefik-internal
    name: traefik
    releaseName: traefik-internal
    repo: https://helm.traefik.io/traefik
    includeCRDs: true
    valuesFile: internal-values.yaml
    version: 41.0.2

resources:
  - ../base
